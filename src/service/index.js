import { getService, createService, createServices } from './service';
import { normalizer, formatter, setMultipleKeyValue, getMultipleKeyValue } from './data';

export {
  // service
  getService,
  createService,
  createServices,
  // data
  normalizer,
  formatter,
  setMultipleKeyValue,
  getMultipleKeyValue,
};
